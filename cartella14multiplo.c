#include <stdio.h>

int main()
{
    int x;
    int y;
    
printf ("inserire un numero\n");
scanf ("%d", &x);
printf ("inserire un numero\n");
scanf ("%d", &y);
if (x/y| 0)
{
    printf ("x è multiplo di y");
}
else 
{
    printf ("x non è un multiplo di y");
}
return (0);
}